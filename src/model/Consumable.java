package model;

/**
 * Интерфейс, представляющий объект, который может быть потреблён.
 * Например, растения или животные, которых можно съесть.
 */
public interface Consumable
{
    double beConsumed();
}
